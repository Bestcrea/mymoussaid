{
  "name": "mymoussaid",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd apps/web && npm install && npm run build",
    "start": "node apps/api/dist/index.js"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
