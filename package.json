{
  "name": "mymoussaid",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd apps/web && npm install && npm run build",
    "start": "echo 'frontend only'"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
